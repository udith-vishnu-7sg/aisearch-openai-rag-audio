# Requirements for the profile data generation script (run locally)
# Note: This script also needs the backend requirements for imports
# Install with: pip install -r app/backend/requirements.txt -r scripts/requirements.txt

# Script-specific dependencies
openai>=1.54.0
pypdf>=4.0.0
requests>=2.28.0
websockets>=12.0
pydub>=0.25.1
